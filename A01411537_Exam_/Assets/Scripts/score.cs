using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class score : MonoBehaviour {

	// Use this for initialization
	public Text Score;
	void Start () {
		//Score.text = destroy.score.ToString();
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
